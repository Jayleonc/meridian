[project]
name = "meridian-codex"
version = "0.1.0"
description = "Meridian Codex — 诊断知识库"
requires-python = ">=3.11"
dependencies = [
    "mcp",
    "pydantic>=2.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
